# 功能台账

新增功能必须先登记再开发；只有实现、服务器门禁与独立审查全部通过后，才将对应项勾选并划掉。

- [x] ~~**1.0.1 当前源码打包**：统一 Node、Cargo、Tauri 与 macOS 包版本为 `1.0.1`，从当前 `main` 生成 Apple Silicon DMG；产物继续明确标注 ad-hoc 签名、未公证且无自动更新。~~
- [x] ~~**1.0.2 当前源码打包与桌面图标**：统一版本源并从最新 `main` 生成 Apple Silicon `.app` artifact 和 GitHub Release DMG；使用织页“纸页 + 交织线”图标，包内代码签名为 ad-hoc linker-signed，未使用 Developer ID、未公证且无自动更新。~~
- [x] ~~**1.0.0 当前源码打包**：统一 Node、Cargo、Tauri 与 macOS 包版本为 `1.0.0`，从当前 `main` 生成 Apple Silicon DMG；产物继续明确标注 ad-hoc 签名、未公证且无自动更新。~~

- [x] ~~**首次使用指导**：首次进入时分步介绍网页导入、资料库、Markdown 编辑、搜索与数据安全；可跳过、可再次打开，并满足键盘操作和可访问性要求。~~
- [x] ~~**Markdown 保真翻译**：文档页提供目标语言与翻译按钮；翻译结果可阅读且不覆盖原文，保留标题、列表、表格、代码块、链接、图片等 Markdown 结构，并明确网络目标与发送范围。~~
- [x] ~~**全局正文字号调整**：提高导航、表单、列表、提示、编辑器和预览正文的默认字号，同时保留标题层级、窄屏布局与可访问性。~~
- [x] ~~**AI 平台预设与应用内密钥配置**：远程 AI 设置提供常用 OpenAI-compatible 平台下拉选择，仅“其他”显示手动端点；API Key 在平台下方输入，桌面端安全持久化到 macOS 钥匙串，Web 端仅保存在当前服务进程内且不写入数据库、日志或导出。~~
- [x] ~~**主界面导航与侧栏收放**：页首辅助入口固定在右侧；Logo 可返回知识库主界面；桌面端知识织片侧栏可通过具名按钮收起和展开，且不绕过未保存修改保护。~~
- [x] ~~**渲染公式剪藏保真**：浏览器扩展与网页提取在正文转换前恢复 KaTeX/MathML 中的原始 TeX，保留行内和块级公式，且只生成文本节点、不执行页面内容。~~
- [x] ~~**ChatGPT 公式源兼容**：浏览器扩展识别当前 ChatGPT/Defuddle 使用的 `data-latex` 公式源；公式位于复制按钮或只读交互容器时仍按原顺序保存为可编辑 TeX，且不保留交互控件内容。~~
- [x] ~~**ChatGPT 原生 MathML 兼容**：浏览器扩展在清理复制按钮和只读交互容器时保留其中不带 TeX 属性的纯 MathML，交由现有提取器转换为行内或块级公式；表单、可编辑区域与视觉副本仍不得进入正文。~~
- [x] ~~**ChatGPT KaTeX 可见层兼容**：浏览器扩展参考已验证导出器，在公式缺少 TeX annotation 或属性时优先读取 `.katex-html` 可见文本，即使节点仍含无源码 MathML；按原上下文保存为行内或块级公式，不重复已有 TeX，也不接受会破坏 Markdown 分隔符的回退文本。裸 MathML 继续交由现有转换器处理。~~
- [x] ~~**文章 LaTeX 公式显示**：本地与 Cloudflare 的统一 Markdown 预览识别 `$…$` 行内公式和 `$$…$$` 块级公式，使用 KaTeX 安全渲染；长块级公式可横向滚动，编辑器原文、导出和存储格式不变。~~
- [x] ~~**知识阅读界面精简**：侧栏只保留搜索与搜索范围，移除组合筛选控件；桌面折叠态提供搜索、全部、收藏、新建文章和回收站快捷按钮；选中知识的标记线与标题增加 6px 间距；文章标题滚出视口后显示返回标题按钮并准确回到标题顶部；关键词派生结果使用可见分隔符展示。~~
- [x] ~~**收藏与回收站操作修正**：云端文章标题区提供收藏/取消收藏按钮，收藏状态由 D1 与 v4 云留档持久化，并以“已收藏”替代“已就绪”；回收站每行明确提示草稿风险并确认后永久删除；相对文章标题向下滚动 240px 后固定悬浮返回标题按钮。~~
- [x] ~~**返回标题真实滚动修复**：云端只读长文使用鼠标滚轮下滑超过标题 240px 后显示按钮，按钮固定在视口右下角并返回标题顶部；测试不得用脚本滚动替代真实滚动。~~
- [x] ~~**独立一级文件夹**：保留多集合分类，同时让每篇知识只归属一个一级文件夹或“未分类”；本地与云端均支持创建、重命名、删除和 revision 保护的移动，且 Markdown、便携包与完整留档不丢失归属。~~
- [x] ~~**统一 UI 基础组件**：以原生语义统一常用按钮、表单、下拉、对话框、确认/输入提示、通知与悬浮信息层；替换主应用浏览器默认弹窗和下拉外观，并保持焦点、键盘及读屏行为。~~
- [x] ~~**紧凑文件夹目录**：左栏以紧凑单行条目展示知识，允许同时展开多个一级文件夹，并支持创建/重命名/删除、拖放与键盘移动，以及悬停 1 秒后的完整元数据卡；移动端保留 44px 触控与对话框移动路径。~~
- [x] ~~**视口固定知识织片**：桌面端左侧知识织片滚动到视口顶部后保持固定且高度不超过视口；内容超出时仅在侧栏内滚动，移动端布局不变。~~
- [x] ~~**文件夹直接目录**：知识织片不再重复展示“全部网页”平铺列表，而是直接展示顶层知识与多个一级文件夹；新网页默认进入顶层，回收站仍保留独立列表。~~
- [x] ~~**顶层目录语义**：根目录仅表示文件夹树顶层，不渲染为可折叠文件夹；未归入文件夹的知识直接显示在顶层，并移除知识行左侧点阵拖动图标，保留整行拖动。~~
- [x] ~~**目录行精简与悬浮行为**：移除知识行的可见状态圆点并将空间让给标题，状态仍向读屏与详情卡提供；详情卡仅在鼠标悬停满 1 秒后显示，点击标题立即关闭且不因焦点常驻。~~
- [x] ~~**编辑器滚动稳定**：只保留 CodeMirror 内部滚动容器，消除对照模式的重复滚动条；自动保存或其他只读状态切换不重建编辑器，光标与滚动位置保持不变。~~
- [ ] **编辑器自动保存防抖**：编辑器连续 20 秒无新输入后才自动保存；输入期间不发送正式文档更新，手动保存、关闭窗口和数据安全操作仍立即落盘。
- [x] ~~**云端留档导入本机**：数据安全页的本机导入可直接选择网页端导出的 `.zhiye-cloud-backup`，服务端将其解析并转换为校验通过的 `.zhiye-backup` 记入留档台账，可再按“恢复此留档”切换数据；云端从未保存网页快照与离线图片，转换后的快照/资源目录为空，属源端设计而非数据丢失。~~
- [x] ~~**云端图片抓取存储与显示**：云端 capture 与浏览器扩展剪藏在正文生成后按预算（单文档 32 张、单张 5 MiB、合计 20 MiB、并发 4）抓取图片，经 SSRF 校验（含每次重定向逐跳校验）后按内容 SHA-256 存入 R2（绑定 `IMAGES`/`zhiye-cloud-images`），把 Markdown 图片改写为 `zhiye://asset/<hash>`；worker 新增同源 `GET /api/assets/:hash` 于 Access 保护下服务内容寻址缓存图（`immutable` + ETag），兼容常见图片 Content-Type 别名，失败沿用宽松类型匹配、保底保留原 URL。网页端与 app 端预览把 `zhiye://asset/<hash>` 渲染为当前源 `/api/assets/<hash>` 缓存图，外部原站热链仍拦截；仅对新抓取页面生效。~~
- [x] ~~**云端图片 URI 预览修复**：Markdown 渲染器仅放行格式严格的 `zhiye://asset/<sha256>` 图片 URI，并将其交给现有同源资源组件渲染；其他未知或危险协议继续由 `react-markdown` 默认安全规则拦截，服务器门禁须覆盖 URI 保留、危险协议拒绝与完整生产构建。~~
- [x] ~~**云端图片随留档离线**：云备份升级为 v5（存储小体积 JSON manifest，导出时逐张从 R2 流式组 ZIP：`manifest.json` + `assets/<hash>`）。云端导出/导入、app 本机导入均能携带图片，使云图片在 app 离线可显示；旧 JSON v1–v4 仍可导入。云端导出在 Access 保护下流式产出（无体积上限，图片内容寻址 immutable），app 导入把 `zhiye://asset/<hash>` 改写为本地资产并建立 `document_assets`/`assets` 就绪映射。导入校验图片内容哈希与大小（内容寻址一致），导出预检缺失图片以 409 干净失败，防解压炸弹。~~
- [x] ~~**云端公开网页与资源兼容修复**：公开抓取优先使用页面声明的 `text/markdown` 入口，避免大体积 HTML 触发 Browser Run 失败；浏览器回退仍保留公网、重定向、大小与超时边界。浏览器扩展剪藏需识别常见 Markdown 图片节点（含引用式、标题、带括号或尖括号 URL），按既有预算缓存到 R2 并改写为 `zhiye://asset/<hash>`；验收覆盖 Docker Docs 示例和含图片的扩展剪藏。~~
- [x] ~~**云端 v5 留档导入兼容重复来源**：本机 1.0.1 可导入真实 `.zhiye-cloud-backup` v5 ZIP；同一来源 URL 的多个云端副本保留为独立文档并生成本地唯一来源标识，图片资产与 `document_assets` 映射完整可恢复。~~
- [ ] **云端快照/图片存储改造**：让云端 capture 抓取原始 HTML 与图片并持久化到 R2（当前云端仅存正文文本、HTML 用完即弃），使云留档能携带快照与资源、与完整本地留档双向等价。仅对新抓取页面生效，现有云端文档仍无快照/图片。作为独立里程碑另行开发。
- [x] ~~**自由 AI 对话**：在预设派生指令后增加“AI 对话”，用户可输入 1–4,000 字符的自由 Prompt，预览准确发送范围后生成独立结果；本地与 Cloudflare 行为一致，不改写正文。~~
- [x] ~~**预设 AI 一键获取**：除“AI 对话”外，摘要、提纲、关键词、翻译及本地标签建议使用“获取”一次点击完成发送与生成，不展示 SEND PREVIEW；后端仍在同一操作内生成并校验预览哈希，结果直接出现在“02 · LEDGER”。~~
- [x] ~~**编辑与筛选界面收尾**：文档标题编辑器固定为单行；共享下拉控件使用织页自有弹层而非浏览器 option 弹层，并保留键盘与读屏操作；桌面侧栏收起后不产生横向滚动。~~
- [x] ~~**全局主题滚动条**：主应用所有纵向与横向滚动区域统一使用易辨识的灰褐滑块，悬停与按下切换朱红层级，并在 forced-colors 下恢复系统高对比度颜色；不改变滚动行为或强制显示。~~
- [x] ~~**显著主题滚动条与目录删除**：滚动条常态即使用朱红滑块与淡朱红轨道，避免与浏览器默认外观混淆；知识目录三点菜单提供“移动到文件夹”和“删除（移入回收站）”，沿用 revision 冲突与未保存修改保护。~~
- [x] ~~**Firefox 主题滚动条兼容**：使用 Firefox 专属能力检测启用标准 `scrollbar-color`，并以真实 Firefox 验证纵横滚动、朱红滑块、淡朱红轨道及 forced-colors 回退。~~
- [x] ~~**Cloudflare 回收站生命周期**：D1 正式文档与抓取任务支持 revision 保护的移入回收站、列表、恢复和永久删除；队列不再发布已删除任务，云留档保留删除状态并兼容旧版本。~~
- [x] ~~**目录快捷新建**：知识织片“＋”菜单可选择创建一级文件夹或空白文章；空白文章默认位于顶层并直接进入编辑，目录行在无复选框时把空间让给标题。~~
- [x] ~~**云留档导出流修复**：R2 留档完成哈希与结构校验后仍以已缓冲字节下载，避免重复读取已消费的对象流导致 HTTP 500。~~
- [x] ~~**完整留档管理**：数据安全页的云端完整留档区域占满可用宽度；每条留档可在确认后删除，并同步删除本地留档目录或云端 R2 对象及记录。~~
- [x] ~~**扩展弹窗滚动收尾**：弹窗不产生页面级横向滚动，正文编辑区保留必要纵向滚动，并与主应用使用相同的朱红轨道、滑块及高对比度回退。~~
- [x] ~~**扩展保存即时刷新**：扩展保存成功后通知已打开且已授权的织页标签页，自动刷新顶层目录与文件夹并显示成功提示；无织页标签页时保存仍正常完成。~~
- [x] ~~**Firefox 正式上架准备**：生成符合 AMO Manifest V3、自托管依赖、固定扩展 ID、最小权限、隐私说明与源代码审查要求的发布包和校验门禁；实际提交与签名以 Mozilla 开发者账户审核为界。~~

## 0.9.2 功能完整收尾

- [x] ~~**长文完整分批翻译**：在 100 万编辑器字符、5,000 个可译段和 64 批的边界内顺序翻译；任一批失败、取消或正文变化均不保存半成品。~~
- [x] ~~**AI 连接测试**：使用固定非文档探针测试当前表单中的远程或可信本地模型，并区分 DNS、TLS、鉴权、限流、模型、协议和超时错误。~~
- [x] ~~**全应用中文错误提示**：按稳定错误码集中显示中文操作建议，不直接向用户暴露英文服务端详情。~~
- [x] ~~**完整留档文件导入导出**：把现有已验证完整备份安全封装为 `.zhiye-backup`，支持换机导入、再次验证和明确恢复。~~
- [x] ~~**可信 localhost Web 模式**：显式启用后允许经 SSH 隧道访问裸地址，同时保留会话、Host、Origin、JSON 与 data-epoch 边界。~~
- [x] ~~**非 root Chromium 部署**：以无登录用户和沙箱化 Chromium 运行 Web 预览，并用真实动态页面验证浏览器回退及进程清理。~~
- [x] ~~**帮助与关于**：复用使用指南和快捷键弹窗，展示快速上手、版本、运行模式、许可、隐私、支持与安全入口。~~
- [x] ~~**受限网页手动摘录**：自动抓取被登录或反爬限制拒绝时，保留来源链接与失败历史，并允许转入 Markdown 编辑器手动保存正文。~~
- [x] ~~**Chrome / Firefox 登录态剪藏扩展**：通过一次性配对码授权扩展读取用户主动打开的当前页面，确认 Markdown 后只写入一个新副本，不读取 Cookie 数据库或上传登录凭证。~~
- [ ] **RC 质量与发布证据**：完成无障碍、提取样本、完整留档 E2E、三次干净门禁、24 小时 soak、受保护 DeepSeek smoke 与未签名 RC 资产验证。

## 云端 Web 迁移

- [ ] **Cloudflare 云端 Web 核心**：以 Workers Static Assets 托管前端、D1 保存云端核心数据，随后接入 R2 留档资源、Browser Rendering 与 Queues 抓取，并以 Cloudflare Access 保护 Web 应用；现有本地 Node/Tauri 路径在功能等价前继续保留。验收边界见 [CLOUDFLARE.md](./CLOUDFLARE.md)。
- [x] ~~**云端配对状态**：首页仅在没有已配对浏览器时显示扩展配对引导，配对或撤销后及时更新。~~
- [x] ~~**D1 文档编辑**：云端文档可切换为标题与 Markdown 编辑模式，保存时校验 revision 并防止静默覆盖。~~
- [x] ~~**云端 AI**：云端安全保存端点绑定密钥，支持连接测试、摘要与翻译，不向 D1、日志或导出包泄露明文密钥。~~
- [x] ~~**云端 AI 密钥跨刷新恢复**：仅在正式云端域名把端点绑定密钥保存到浏览器站点存储；刷新后恢复，切换端点不得误发，删除后立即失效；缺少密钥时在派生预览前提示。~~
- [x] ~~**云端完整翻译输出**：翻译使用足够的输出预算并识别供应商截断，保留 Markdown 结构校验；发送预览不再要求额外勾选确认。~~
- [x] ~~**云端文本片段翻译**：模型只翻译带 ID 的自然语言片段，织页将译文安全插回原 Markdown；模型无法改写标题层级、链接目标、代码、HTML 或 Front Matter。~~
- [x] ~~**R2 云端留档**：用户可创建、下载、导入并明确恢复云端留档，失败不留半成品。~~
- [x] ~~**云端直接抓取**：公开 HTTP(S) 网址经 Queue 异步抓取，需要脚本时使用 Browser Rendering，继续拒绝私网、回环和越权地址。~~
