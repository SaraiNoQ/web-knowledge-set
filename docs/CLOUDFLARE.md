# Cloudflare 云端 Web 迁移

## 状态

Cloudflare Web 已部署在 `https://zhiye.sarainoq.cn`，并由 Access 保护。线上已支持扩展剪藏、搜索、阅读、Markdown 编辑、可选 AI、R2 留档与 Queue/Browser Run 公开网页抓取。

## 目标架构

| 能力 | Cloudflare 服务 | 迁移边界 |
| --- | --- | --- |
| Web 界面 | Workers Static Assets | Worker 与前端同域发布，避免独立静态服务器。 |
| 文档、一级文件夹、标签、设置等云端核心数据 | D1 | 使用显式迁移；不直接复用本地 SQLite 文件。 |
| 图片、快照与完整留档 | R2 | 对象由数据库记录引用；API Key 不写入对象或数据库。 |
| 网页抓取 | Browser Rendering + Queues | 浏览器任务异步执行；继续拒绝私网、回环和受限地址。 |
| Web 访问控制 | Cloudflare Access | 默认保护应用与普通 API；仅为剪藏扩展的最小写入端点设置独立、受限入口。 |

## 分阶段实施

1. **Workers Static Assets + D1 云核心**：已建立 Worker、前端静态资源和 D1 数据模型，支持扩展剪藏、搜索、阅读和标题/Markdown 编辑；本里程碑在同一 D1 核心增加一级文件夹，仍从空云端知识库开始，不自动复制本地数据。完成部署门禁前不得把新增能力描述为已上线。
2. **R2 资源与留档**：私有 `zhiye-cloud-backups` 与 `zhiye-cloud-images` bucket 已绑定，支持创建、校验、导入导出、明确恢复和确认删除。`.zhiye-cloud-backup` v5 保存文件夹、文档归属、收藏、回收站状态、AI 结果与引用图片；导入器继续接受 v1/v2/v3/v4 JSON，旧归档按兼容默认值恢复。
3. **Browser Run + Queues 抓取**：`zhiye-cloud-capture` 已同时绑定生产者和消费者；消费端优先读取页面声明的 `text/markdown` 入口，找不到时再使用 Browser Run Markdown Quick Action，并继续执行逐跳公网校验和大小限制。
4. **Access 与扩展迁移**：Web 由 Access 保护；独立的 `clip.sarainoq.cn` Worker 只接受扩展配对和剪藏写入，令牌不能读取、搜索、删除或导出知识库。
5. **功能等价与切换**：逐项验证编辑、搜索、导入导出、备份恢复、AI 与剪藏后，才允许将 Web 默认入口切到 Cloudflare。

本地 Node 服务和 Tauri 桌面端继续使用本地 SQLite、文件系统与 macOS 钥匙串。它们不是 Cloudflare 迁移的替代品，也不会在云端功能等价前删除。

本地与 Cloudflare Web 都只提供独立一级文件夹：新文档默认进入文件夹树顶层（数据中以空 `folder_id` 表示），顶层本身不是文件夹；删除文件夹会保留文档并移回顶层。文件夹不嵌套，也不支持人工排序；可多选集合继续作为另一套分类。

Cloudflare Web 的正式文档和抓取任务均支持 revision 保护的回收站生命周期。删除抓取中的任务会停止其队列发布；恢复后以失败态保留，由用户明确重试。

两端功能等价不代表云同步。本地 SQLite 与 Cloudflare D1/R2 不会自动互传文件夹或文档，跨端迁移仍必须由用户明确导出和导入。

云端图片只对新抓取或新剪藏正文按预算缓存；每张最多 10 MiB、每篇 64 张、合计 64 MiB。缓存成功后正文使用 `zhiye://asset/<sha256>`，Web 同源资源路由从 R2 提供内容；抓取失败仍保留原始 URL，但 Access/CSP 不允许预览阶段回连原站。预算须覆盖动图密集的长文：单张 10 MiB 与 App 离线图片的单张上限一致；单篇合计 64 MiB 只是留在云留档 96 MiB 解压上限之内，该上限按整份归档计算，多篇媒体密集文档仍会超出并需要分次留档。

## 安全与发布前提

- Cloudflare API Token、Access 凭据和用户数据均不提交到仓库。云端 AI Key 只保存在 Web 域的浏览器站点存储与扩展本地存储中，只随显式测试、显式生成或用户已启用的剪藏标题请求发给 Worker，不写入 D1、R2 或诊断数据。
- 使用自定义域名且关闭可绕过 Access 的公开 Worker 地址；Access 策略默认拒绝。示例 Wrangler 配置故意不带 route：先创建 Access 应用和所有者 allow 策略，再在未跟踪的 `cloud/wrangler.web.jsonc` 中添加同一域名。
- 发布门禁必须从未登录浏览器验证 `/` 和 `/api/documents` 都被 Access 拒绝，然后再以所有者身份验证空库首屏。没有这项证据不得添加公开 route。
- 部署前需要 Cloudflare 账户授权、受管域名、D1/R2/Queues/Browser Rendering 资源标识和 Access 配置。缺少其中任一项时，只能完成源码与配置准备，不能宣称已上线。
- 本地数据迁移必须通过已验证的导出与用户明确导入，不从服务器目录静默复制，也不把 Cloudflare Web 当作同步副本。

## 部署记录

- 2026-09-15 · `02924c0` · 知识地图面板改为整屏高度。原先 `.knowledge-map-host` 与 `.workspace.is-map-mode .reader-panel` 用 `calc(100vh - 246px)` 估算高度，而该偏移并不等于顶部外壳与前置区块的实际高度，导致面板永远比窗口矮 246px、顶边无法落到窗口顶部；现改为整屏高度（`100vh`/`100dvh` 回退写法）并让页面滚动，与论文阅读器一致，≤820px 与 ≤560px 断点不再各自覆盖高度、只保留各自最小高度（窗口更矮时保持最小高度并保留页面滚动）。修复前用 Playwright 量到面板正好比视口少 246px；修复后 1440/1000/800/560 四种宽度滚到底时面板顶边为 0、底边与窗口底部一致、面板高度等于视口高度，内部容器与画布区底边同样落在窗口底部 2px 内且无横向溢出，并另抓 1440/820/560 三张截图人工核对。同一批提交还修了两个既有 e2e 失败：`2819f1b` 把语义面板嵌进 AI 设置弹窗后「保存密钥」出现两个同名按钮，现把定位收敛到 `.ai-keychain`。无 D1 迁移（部署前 `wrangler d1 migrations list --remote` 报无待执行迁移）；`zhiye-web` Version ID `86284342-e7f0-44a6-9b83-ca903780b9dd`，`zhiye-clip` Version ID `67f1cbab-a2b8-4b46-b27f-f9020f8c46c2`。部署前 Node 24.19.0 冻结安装、`check`、`test`（202 通过、1 跳过）、`build`、`cloud:check`（42 通过）、`cloud:bundle` 与 `e2e/app.spec.ts`（26 通过，唯一失败为已记录的「搜索范围」下拉间歇 flake）全部通过。未登录边界复验：Web `/`、`/api/documents`、`/health`、`/api/settings/semantic`、`POST /api/semantic/test`、`POST /api/semantic/index-step`、`POST /api/papers` 均为 Access 302；`clip.sarainoq.cn` 无 Origin 403 `EXTENSION_ORIGIN_REJECTED`、非扩展 Origin 403、带扩展 Origin 无令牌 401 `EXTENSION_UNAUTHORIZED`、携带 `x-zhiye-llm-key` 的预检 204。
- 2026-09-15 · `47f2d7e` · 修复 `POST /api/semantic/test` 在 Cloudflare 上恒定报 `SEMANTIC_NETWORK_ERROR` 的根因：`shared/semantic.ts` 对供应商使用 `fetch(..., { redirect: "error" })`，而 workerd 不实现该取值，会在发出请求前抛 `TypeError`（`one of "follow" or "manual"`），被兜底归类成"供应商不可达"——因此与密钥、出网都无关；Node 支持该取值，`cloud/` 全部测试又只在 Node 下运行，所以类型检查与 `cloud:check` 全绿而仅线上必挂。现改为 `redirect: "manual"` 并显式拒绝 3xx（新增不可重试的 `SEMANTIC_REDIRECT_REJECTED`），密钥仍绝不跟随重定向；`cloud/paper.ts` 的论文 URL 导入 PDF 属同一处缺陷，一并修复。已在构建机用真实 workerd（`wrangler dev`、同等 `compatibility_date`）复现并验证：修复前部署版形态、无信号形态与纯 `redirect:"error"` 三种全部抛 redirect `TypeError`，修复后真实供应商返回 401（`SEMANTIC_AUTH_FAILED`）、注入 302 返回 `SEMANTIC_REDIRECT_REJECTED`。无 D1 迁移（部署前 `wrangler d1 migrations list --remote` 报无待执行迁移）；`zhiye-web` Version ID `ea82df7b-9d6a-49ea-90da-0d825dafa78f`，`zhiye-clip` Version ID `33882a6f-6e1a-440d-aec6-a0ff2e433d79`。部署前 Node 24.19.0 冻结安装、`check`、`test`（202 通过、1 跳过）、`build`、`cloud:check`（42 通过）、`cloud:bundle` 与语义 Playwright（3/3）全部通过。未登录边界复验：Web `/`、`/api/documents`、`/health`、`/api/settings/semantic`、`POST /api/semantic/test`、`POST /api/semantic/index-step`、`POST /api/papers` 均为 Access 302；`clip.sarainoq.cn` 无 Origin 403 `EXTENSION_ORIGIN_REJECTED`、非扩展 Origin 403、带扩展 Origin 无令牌 401 `EXTENSION_UNAUTHORIZED`、携带 `x-zhiye-llm-key` 的预检 204。
- 待补：以站点所有者会话在生产点一次「语义关联 → 连接测试」。本轮只证明运行时不再提前抛错（构建机真实 workerd 下已到达供应商并得到 401），Cloudflare 机房出口能否连到 `https://api.siliconflow.cn/v1/embeddings` 仍需一次线上请求证实。
- 2026-09-14 · `baed6e6f06c0adaea8de2dcc5e275db3ff2b8d34` · 知识地图全库浏览与语义索引。先在生产 `zhiye-cloud` D1 应用 `0011_semantic_indexes.sql` 一次（数据库 ID `b3ac6ecc-dcf1-4bc7-a269-35c889b2ebe6`），只新增索引/分段缓存表和默认关闭的设置；迁移后确认无待执行迁移，并只读核实 `enabled:false, model:BAAI/bge-m3`。`zhiye-web` Version ID `97b9be73-e4fb-410f-89ae-7a39fab11239`，`zhiye-clip` Version ID `b74bf18e-798a-462d-b05c-045dbc3d2964`。部署前 Node 24.19.0 冻结安装、`check`、`test`（199 通过、1 项 Linux root 沙箱限制跳过）、`build`、`cloud:check`（42 通过）、双 Worker `cloud:bundle` dry-run、知识地图/无障碍 Playwright（6/6）和许可证检查通过。部署后无 Access Cookie 的 `/`、`/api/documents`、`/health`、语义设置 API 均返回 302；clip Worker root/无 Origin 写入返回 403、扩展格式 Origin 无令牌返回 401、预检返回 204。
- 2026-09-14 · `962eae5` · 语义供应商传输错误分类与脱敏诊断。无新增 D1 迁移；超时改报 `SEMANTIC_PROVIDER_UNAVAILABLE`，即时 fetch 异常仍报 `SEMANTIC_NETWORK_ERROR`；Worker 日志只记录白名单错误名及 `timedOut`，不记录密钥、原文或错误消息。`zhiye-web` Version ID `d053ec22-1227-416d-ae70-b265621ee649`，`zhiye-clip` Version ID `41c0daca-5e3a-4a62-9c7b-a39eeddf9137`。部署前 Node 24.19.0 冻结安装、`check`、`test`（200 通过、1 项 Linux root 沙箱限制跳过）、`build`、`cloud:check`（42 通过）、双 Worker dry-run 和语义设置 Playwright（2/2）通过；生产库的 `0011_semantic_indexes.sql` 仍为已应用状态，且无待执行迁移。用户确认使用 `BAAI/bge-m3` 且几秒内失败；一次已登录的 `/api/semantic/test` 生产请求触发日志 `TypeError, timedOut:false`，表示 Cloudflare Worker 的 provider fetch 在收到 HTTP 响应前失败，尚不能区分 Cloudflare 出网与 SiliconFlow 对 Worker 出口的连接问题。构建服务器对固定 SiliconFlow 地址的无密钥探针得到预期 HTTP 401，只能证明构建服务器可连通，不等同于 Worker 出网验证。语义索引保持关闭。
- 待补：以站点所有者会话验证线上资料读写、R2 图片读取和实际扩展配对/剪藏；当前只完成 Access 未认证边界和 clip 的 CORS/未授权边界验证。
- 2026-09-10 · `c89df85` · 抓取后自动中文标题。`zhiye-web` Version ID `b174f3ec-74c2-4865-bbdc-d76f0983d408`，`zhiye-clip` Version ID `819defe0-7f35-4c68-aa55-7dc89fea3fcc`；无 D1 迁移。从 `123.207.203.208:/root/dev/zhiye` 用 `cloud/wrangler.web.jsonc` 与 `cloud/wrangler.clip.jsonc` 部署，部署前 `check`/`test`/`build`/`cloud:check`/`cloud:bundle` 全部通过。
- 未登录边界复验：`/`、`/api/documents`、`/health` 与 `POST /api/documents/<id>/auto-title` 均返回 Access 302，`clip.sarainoq.cn` 剪藏端点返回 403。
- 2026-09-11 · `4ae2850` + `8cf7246` · 左侧目录实时刷新与扩展 `0.3.4`。`zhiye-web` Version ID `5a3282be-9bad-4633-bc64-c2c463c5bffc`；`zhiye-clip` 本轮无改动，保持 `819defe0-7f35-4c68-aa55-7dc89fea3fcc`；无 D1 迁移。部署前 `check` 与 `build` 通过、`test` 148 通过 1 跳过。该版本同时把 `0.3.4` 的 Chrome/Firefox 下载包发布到“帮助 → 浏览器扩展”。未登录边界复验同上（`/`、`/api/documents`、`POST /api/documents/<id>/auto-title` 均为 Access 302）。
- 2026-09-11 · `f85345d` · 标题合规与扩展密钥修复。`zhiye-web` Version ID `dfb40684-c4cb-4e8b-936e-c82540ce4841`，`zhiye-clip` Version ID `adf33b68-1665-4c8c-a83c-4234c28e3862`；无 D1 迁移。部署前 `check`、`test`（153 通过、1 跳过）、`build`、`cloud:check`（28 通过）、`cloud:bundle`、`firefox:amo`（0.3.5 通过）全部通过。未登录边界复验：`/`、`/api/documents`、`/health` 与 `POST /api/documents/<id>/auto-title` 均为 Access 302；`clip.sarainoq.cn` 无 Origin 剪藏请求 403 `EXTENSION_ORIGIN_REJECTED`，带扩展 Origin 但无令牌 401 `EXTENSION_UNAUTHORIZED`，携带 `x-zhiye-llm-key` 的预检 204。
- 2026-09-11 · `9ea2023` · 论文对照阅读上线。先在现有 `zhiye-cloud` D1 应用唯一待执行的 `0009_cloud_papers.sql`，仅新增论文字段、表和资源关联，未删除或重写既有数据；迁移后远端显示无待迁移、13 张表。`zhiye-web` Version ID `b103d0f0-6bbc-4294-ac2e-c31abd2db4bf`，`zhiye-clip` Version ID `45702c2c-d134-42de-9f01-2c7cf4e4fae5`。部署前 Node 24.19.0 `check`、`test`（155 通过、1 跳过）、`build`、`cloud:check`（29 通过）、`cloud:bundle` 全部通过；未登录边界复验：Web `/` 与 `/api/documents` 返回 Access 302，`clip.sarainoq.cn/` 返回 403。
- 2026-09-11 · `b237840` · 论文 PDF 不兼容错误提示、云端失败详情回显与阅读器头部间距修复。无 D1 迁移，远端保持 13 张表且无待迁移；`zhiye-web` Version ID `e2e8da6a-47f2-4c3a-aa41-251f43361160`，`zhiye-clip` Version ID `b6b87d64-63f6-4182-a567-886b6407aa19`。部署前 Node 24.19.0 `check`、`test`（157 通过、1 跳过）、`build`、`cloud:check`（30 通过）、`cloud:bundle`、论文 Playwright E2E（2/2）全部通过；未登录边界复验：Web `/` 与 `/api/documents` 返回 Access 302，`clip.sarainoq.cn/` 返回 403。
- 2026-09-11 · `98837e4` · 论文回收站删除、共享 PDF 清理与服务端 document revision 修复。无 D1 迁移，生产 D1 确认无待执行迁移；`zhiye-web` Version ID `50f6ecaf-b389-4e87-8437-12899da8a2a7`，`zhiye-clip` Version ID `2675f963-b5ef-4b5d-a717-61369415453e`。部署前 Node 24.19.0 `check`、`test`（161 通过、1 跳过）、`build`、`cloud:check`（32 通过）、`cloud:bundle`、论文 Playwright E2E（2/2）全部通过；未登录边界复验：Web `/`、`/api/documents` 与 `/health` 返回 Access 302，`clip.sarainoq.cn/` 返回 403。
- 2026-09-12 · `ef03da7` · 论文页图通道与分批提取。先在现有 `zhiye-cloud` D1 应用唯一待执行的 `0010_cloud_paper_content_mode.sql`（`cloud_paper_extractions` 仅新增一个可空 `content_mode` 列，旧版本 Worker 会忽略它，因此迁移先于代码部署是安全的）；迁移后远端显示无待迁移。`zhiye-web` Version ID `64425185-71de-45ac-911e-e24dca5a94d7`，`zhiye-clip` Version ID `ece82d4d-4313-4285-8b60-c1e45feab114`。部署前 Node 24.19.0 `check`、`test`（170 通过、1 跳过）、`build`、`cloud:check`（33 通过）、`cloud:bundle`、论文 Playwright E2E（2/2）全部通过。未登录边界复验：Web `/`、`/api/documents`、`/health`、`GET /api/papers/<id>/extraction-plan`、`GET /api/paper-tasks/<id>`、`POST /api/documents/<id>/auto-title` 与 `PUT /api/papers/<id>/pages/1/image` 均为 Access 302；`clip.sarainoq.cn` 无 Origin 403 `EXTENSION_ORIGIN_REJECTED`、非扩展 Origin 403、带扩展 Origin 无令牌 401 `EXTENSION_UNAUTHORIZED`、携带 `x-zhiye-llm-key` 的预检 204。
- 2026-09-12 · `3d584de` · 截断批次修复。页图通道按批请求时，模型可能耗尽输出预算后返回空 `content`；此前云端把它当成协议错误直接抛出，使二分收窄逻辑无法执行，整篇提取以 `LLM_INVALID_RESPONSE` 失败。现在带 `finish_reason` 而无内容的回答按可收窄的截断上报，并对 DeepSeek 端点显式关闭思考模式（与自动标题一致）。无 D1 迁移；`zhiye-web` Version ID `8b2afc4b-1c02-487a-90da-c222e018f1a6`，`zhiye-clip` Version ID `edd96149-049b-43c4-ab81-8f8af461a85d`。部署前 Node 24.19.0 `check`、`test`（171 通过、1 跳过）、`build`、`cloud:check`（34 通过）、`cloud:bundle`、论文 Playwright E2E（2/2）全部通过。未登录边界复验：Web `/`、`/api/documents`、`/health`、`GET /api/papers/<id>/extraction-plan`、`GET /api/paper-tasks/<id>` 与 `PUT /api/papers/<id>/pages/1/image` 均为 Access 302；`clip.sarainoq.cn` 无 Origin 403、非扩展 Origin 403、带扩展 Origin 无令牌 401。
- 2026-09-13 · `4510116` · 论文阅读器布局与原文缩放。页码列去掉竖排标签并改为自身滚动；原文/译文分栏可拖动（20%–80%）或用方向键调整，两栏各自滚动；原文支持 60%–300% 缩放与重置；删除右侧论文信息栏。纯前端改动，无 D1 迁移；`zhiye-web` Version ID `21d8167e-9a42-46af-a86b-23f87ab115d7`，`zhiye-clip` Version ID `2c2c5720-94c6-402a-badc-c8fd001d970c`。部署前 Node 24.19.0 `check`、`test`（171 通过、1 跳过）、`build`、`cloud:check`（34 通过）、`cloud:bundle` 全部通过，论文 Playwright E2E 连跑 5 次全绿（含页码列内部滚动、两栏内部滚动、缩放重绘、拖动与键盘分栏、≤760px 堆叠不溢出、信息栏不再渲染等断言）。未登录边界复验：Web `/`、`/api/documents`、`/health` 均为 Access 302；`clip.sarainoq.cn` 无 Origin 403、带扩展 Origin 无令牌 401。
- 2026-09-13 · `e848d79` · 阅读器高度按实际外壳测量。此前高度写成 `calc(100vh - 264px)`，而顶部外壳高度随窗口宽度变化（实测顶部位置 1440px 宽为 267px、1000px 为 281px、800px 为 319px），导致阅读器在不同宽度下分别超出 2/17/55px，滚动后又在下方留出空白。现在改为运行时测量自身顶部位置并写入 CSS 变量，窗口缩放与外壳变化用 ResizeObserver 重测；打开论文时工作区高度即为阅读器高度、资料库栏改为列内滚动，整页不再滚动。纯前端改动，无 D1 迁移；`zhiye-web` Version ID `741be5a9-48ca-4ffa-badb-2fdf388e25cb`，`zhiye-clip` Version ID `539517eb-404a-4225-a089-968a59e323bf`。部署前 Node 24.19.0 `check`、`test`（171 通过、1 跳过）、`build`、`cloud:check`（34 通过）、`cloud:bundle` 全部通过；论文 Playwright E2E 连跑 2 次全绿，新增断言要求 1440/1000/800 三种宽度下阅读器底边与视口底边相差不超过 2px 且整页不滚动（旧代码在此分别失败 2/17/55px），另有探针确认资料库栏底部控件在列内仍可滚到。未登录边界复验：Web `/`、`/api/documents`、`/health` 均为 Access 302；`clip.sarainoq.cn` 无 Origin 403、带扩展 Origin 无令牌 401。
- 2026-09-13 · `3414f7f` · 阅读器可滚到全屏。上一版把整页滚动关掉后，顶部外壳再也滚不走，论文无法像文章阅读器那样铺满窗口；现在阅读器高度改为整屏（`100dvh`）并恢复页面滚动，文档总高为「外壳 + 一屏」，滚到底时阅读器顶边落在窗口顶部、底边落在窗口底部，任何滚动位置都不会在下方留白。同时删除上一版运行时测量与工作区锁定代码，并修掉论文 e2e 首个用例的通配步骤竞态。纯前端改动，无 D1 迁移；`zhiye-web` Version ID `54361722-2f15-4d71-81ab-4eae6fc7d51a`，`zhiye-clip` Version ID `b259c885-4e75-4c16-9c76-6d054e74d7e7`。部署前 Node 24.19.0 `check`、`test`（171 通过、1 跳过）、`build`、`cloud:check`（34 通过）、`cloud:bundle` 全部通过；论文 Playwright E2E 连跑 3 次全绿，断言在 1440/1000/800 三种宽度下要求滚到底后阅读器顶边与窗口顶部、底边与窗口底部相差均不超过 2px，且页面确实可滚动。未登录边界复验：Web `/`、`/api/documents`、`/health` 均为 Access 302；`clip.sarainoq.cn` 无 Origin 403、带扩展 Origin 无令牌 401。
- 2026-09-13 · `8dfc0bf` · 页图批次解析放宽与失败可诊断。此前批次解析要求回包必须是 `pages` 数组、长度与顺序一致且页码为数字，模型对单张图片返回别的合理形状（裸页面对象、以页码为键、字符串页码、整页图片的空 `pages`）时会以「页码与请求不一致」整篇失败，且错误信息不记录原文、无法定位。现在页码校验改为按页码集合相等（顺序不敏感、接受数字字符串），仍严格拒绝错页/缺页/重复页；单页请求收到空回包时该页以空内容块保留；失败信息附带模型原文开头（≤300 字）。无 D1 迁移；`zhiye-web` Version ID `c141693b-8f8b-46f2-8501-8204d7e54f73`，`zhiye-clip` Version ID `f0a08508-2868-4a82-99b8-3baa037489a9`。部署前 Node 24.19.0 `check`、`test`（174 通过、1 跳过）、`build`、`cloud:check`（34 通过）、`cloud:bundle`、论文 Playwright E2E 全部通过。未登录边界复验：Web `/`、`/api/documents`、`/health` 均为 Access 302；`clip.sarainoq.cn` 无 Origin 403、带扩展 Origin 无令牌 401。
- 2026-09-13 · `5624fde` · 阅读器标题栏可滚走，正文占满全屏。此前「一屏高」加在整个阅读器上，标题栏（返回资料库、标题、作者、编辑译文）固定占住顶部，滚到底后正文仍只剩一屏减去标题栏的高度。现在一屏高改由内容区（页码列、工具栏、两栏）承担，标题栏按正常文档流排在其上方：滚到底时标题栏完全移出视口，内容区顶边落在窗口顶部、底边落在窗口底部；向上滚动即可取回标题栏操作。无 D1 迁移；`zhiye-web` Version ID `25f71717-e801-4d80-96dd-da47840d4729`，`zhiye-clip` Version ID `7ff9218d-cbd0-4983-8090-9cc578850fa9`。部署前 Node 24.19.0 `check`、`test`（174 通过、1 跳过）、`build`、`cloud:check`（34 通过）、`cloud:bundle` 全部通过；论文 Playwright E2E 连跑 2 次全绿，断言在 1440/1000/800 三种宽度下要求内容区高度等于视口、滚到底后标题栏底边不超过视口顶端 1px、内容区顶边≈0 且底边≈视口底边。未登录边界复验：Web `/`、`/api/documents`、`/health` 均为 Access 302；`clip.sarainoq.cn` 无 Origin 403、带扩展 Origin 无令牌 401。
- 2026-09-13 · `65ee4c2` · 知识织片视图收敛为一行四项（全部/收藏/回收站/论文）。新增 `kind`（`article`/`paper`）文档过滤：本地服务与云端 Worker 同样白名单校验、SQL 绑定参数，未知值返回 400 `INVALID_FILTER`；「论文」视图只列论文，且云端不再合并待抓取任务（本地 schema 把待抓取队列记为 `article` 文档，两端因此返回一致行）；过滤器由当前视图推导而非另存一份状态。纯接口与前端改动，无 D1 迁移（部署前远端确认无待执行迁移）；`zhiye-web` Version ID `febff36b-3a8a-4009-b300-3e54fff16a30`，`zhiye-clip` Version ID `7efbac67-4e4b-49fd-aa96-749be5c1c0ec`。部署前 Node 24.19.0 `check`、`test`（174 通过、1 跳过）、`build`、`cloud:check`（34 通过）、`cloud:bundle`、论文 Playwright E2E（3 通过）与侧栏视图 E2E 全部通过。未登录边界复验：Web `/`、`/api/documents`、`/health` 均为 Access 302；`clip.sarainoq.cn/api/browser-extension/clips` 无 Origin 403 `EXTENSION_ORIGIN_REJECTED`、非扩展 Origin 403、带扩展 Origin 无令牌 401 `EXTENSION_UNAUTHORIZED`，`/api/browser-extension/pair` 空请求 400，携带 `x-zhiye-llm-key` 的预检 204。`e2e/app.spec.ts` 全量运行当时仍有 4 个既有失败（首次引导、批量导入预览、AI 生成、导入/恢复/导出），已用改动前的同一份源码在同一构建机上复现完全相同的 4 个失败，属既有问题；这 4 项已在下一条记录中修复。
- 2026-09-13 · `04514ae` · 修复 `e2e/app.spec.ts` 的四个既有失败，并收尾视图收敛遗留的失效状态与文案。四个失败同源：该文件的所有用例共用同一个服务端进程与数据库，用例却隐式依赖此前用例留下的库状态。首次引导用例改为先用 `PUT /api/settings/onboarding` 复位自己的标记再打开页面；`logo-return` 采集用例结束时从回收站永久删除自己创建的知识（它的离线图片缓存此前会进入后续「导出全部」使资源计数为 1，并让它自己的标题在目录中出现两次，与导入用例的重名断言冲突）；AI 生命周期用例改为显式写入自己断言所依据的设置（`enabled: false`、`target: remote`，并原样保留 remote/local 端点值供后续步骤使用），不再继承为论文用例准备的 `enabled: true` 本地种子——该种子不能改，`server/paper.ts` 在未启用时拒绝提取。论文 Playwright 用例同样改为自行声明「已启用可信本地端点」这一前提，因此上述用例即使中途失败也不会再连带论文用例一起失败。同时删除视图收敛后已无法取到非中性值的六个前端筛选状态（`status`/`archived`/`unorganized`/`captureMode`/`from`/`to`）及其序列化；服务端与云端的同名过滤参数及其实现在 API 层保留不变，被删除的只是前端始终按中性值发送的部分。使用指南第 3 步仍写着「快速查看最近、收藏、失败或回收站内容」，与本轮已收敛的四项视图不一致，一并改为「全部、收藏、回收站与论文，一键切换」。`e2e/app.spec.ts` 23 项全绿（修复前 19 通过 / 4 失败），论文 Playwright E2E 3 项通过，Node 24.19.0 `check`、`test`（174 通过、1 跳过）、`build`、`cloud:check`（34 通过）、`cloud:bundle` 全部通过。无 D1 迁移；本轮未重新部署——只改了测试、失效状态清理与一处说明文案，请求参数流不变，生产仍为 `febff36b-3a8a-4009-b300-3e54fff16a30` / `7efbac67-4e4b-49fd-aa96-749be5c1c0ec`。
- 待补：`e2e/app.spec.ts` 的「返回知识库主界面并收放侧栏」用例在全量运行时约每 3–4 次失败 1 次：在「搜索范围」下拉上用键盘把「仅标题」改成「仅正文」时，菜单会在 Enter 之前关闭，取值停在原选项。单独重复运行 9 次、连同其前置用例重复 21 次都未复现，因此尚未定位根因；已确认与本轮改动无关（该用例与本轮 diff 不重叠）。定位它需要一次带 trace 的复现。
- 待补：以所有者身份用真实 DeepSeek 端点重新提取一篇长论文，确认页图通道产出分页对照（`docs/FEATURES.md` 的页图通道验收项因此仍未勾选）。
- 待补：以所有者身份完成抓取、扩展剪藏与本地抓取三端的真实标题替换验证；`0.3.5` 已在 AMO 自签名（unlisted，见 [FIREFOX_AMO.md](./FIREFOX_AMO.md)），若要走商店安装仍需提交审核并更新安装链接。

相关官方资料：[Workers Static Assets](https://developers.cloudflare.com/workers/static-assets/)、[D1 限制](https://developers.cloudflare.com/d1/platform/limits/)、[R2](https://developers.cloudflare.com/r2/)、[Browser Run](https://developers.cloudflare.com/browser-run/)、[Queues](https://developers.cloudflare.com/queues/reference/how-queues-works/)、[Cloudflare Access](https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/authorization-cookie/validating-json/)。
