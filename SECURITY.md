# 安全说明

本文描述当前预发布版本的信任边界、已有控制和剩余风险。织页按单用户本机应用设计，不是可暴露到局域网或互联网的服务器。

## 威胁模型与控制

### 本地服务与会话

Node 服务只绑定 `127.0.0.1`，拒绝非 localhost `Host`、跨源写请求和非 JSON 写入。启动时生成一次性随机令牌，兑换为 `HttpOnly; SameSite=Strict` 会话 Cookie 后从地址栏移除；API 无有效会话会返回未授权。桌面端使用受限 CSP 和最小 Tauri capability，前端不能任意启动 shell 或 sidecar。

仍需信任当前系统用户和操作系统。以相同用户运行、能读取进程或应用文件的恶意软件不在此边界内；localhost 也不等于抵御已攻陷主机。不要转发或公开本地服务端口。

### 不可信网页与出站请求

网页 URL、每次重定向、浏览器请求和离线图片请求都会重新解析并只允许公网单播地址；连接固定到已验证 IP，以降低 DNS rebinding 风险。回环、私网、链路本地、组播、保留地址、IPv4-mapped IPv6 和云元数据地址会被阻止。浏览器回退经本机安全代理，使用临时 context，不导入 Cookie，并阻止权限、下载、Service Worker、WebSocket、字体、图片和媒体。

原始 HTML 只作为压缩快照保存，从不直接渲染。Markdown 预览禁用原始 HTML，只允许安全链接协议；外链隔离打开，本地图片按内容哈希和校验后的 MIME 提供，并带 CSP 与 `nosniff`。这些措施降低 XSS 和本地文件读取风险，但提取结果仍是不可信文本，用户不应把其中的命令或提示当作可信指令。浏览器回退仍会在 Chromium 沙箱内执行页面脚本并允许其请求公网目标，网络预算与临时 context 不能消除浏览器未知漏洞或目标站点副作用。

抓取只面向公开网页，不支持登录态、浏览器 Cookie、付费墙、验证码绕过或内网资源。目标网站仍能观察请求 IP、时间和路径，也可能按其条款限制自动读取。

### 文件、导入导出与数据破坏

数据库、快照、资源、备份、恢复暂存和启动器路径均限制为受控目录及规则化文件名；拒绝符号链接和越界路径。便携 ZIP 导入限制条目数、单项/总大小、压缩比、路径和文件类型，先在暂存区完整校验，再写入正式数据。深链只接受长度受限、不含凭据的 HTTP/HTTPS URL，Node 层仍会执行完整 URL 检查。

备份使用 SQLite 一致性副本和版本化 SHA-256 清单，恢复前校验并先保留当前数据。迁移只向前追加，较旧应用不会被允许打开较新的数据库 schema。SHA-256 用于完整性而非来源认证；有本地写权限的攻击者仍可同时替换备份及其清单。

### AI 与密钥

AI 默认关闭，生成前必须逐篇展示准确发送范围并再次确认。远程端点只允许 HTTPS 和公网地址，不跟随重定向；本地端点只允许显式信任的 loopback 地址。请求有时限和响应大小限制，模型输出作为不可信 Markdown 渲染，图片请求被阻止，结果与人工正文分离。

远程 API 密钥不进入 SQLite、前端响应、导出或诊断包。macOS 桌面端把密钥存入系统钥匙串；Web 模式从进程环境读取并立即从 `process.env` 删除，但进程内存和父进程环境仍属于系统信任边界。任何模型端点都能读取用户明确发送的正文，并可能返回错误或恶意内容。

### 日志、依赖与发布

本地诊断日志采用严格 allowlist、大小轮转、7 天保留和仅当前用户权限；未知字段或未知错误码不会写入。仓库锁定依赖，生成第三方许可证清单和 SBOM，并在 CI 执行 JavaScript/Rust 漏洞审计。

当前 `0.9.0` 仍是发布候选源码构建：签名、公证、stapled DMG、签名更新 manifest 和应用内 updater 尚未完成。未经签名的构建无法提供正式发行版的来源保证；只应从本仓库和已核对的提交构建。不要下载第三方声称的织页安装包。

## 资源与拒绝服务边界

静态 HTML 最多 5 MiB、5 次重定向和 15 秒；浏览器回退最长 30 秒，代理网络预算 25 MiB，最终 HTML 最多 5 MiB。离线图片默认单张 10 MiB、每篇 100 张、总计 100 MiB、并发 3。JSON、导入文件、便携包、模型输入与输出也有固定上限。

这些限制降低意外耗尽，不保证抵御能持续提交任务的同机恶意用户、复杂的压缩/解析炸弹或上游库漏洞。采集队列保持单任务执行；出现异常资源增长时暂停队列、退出应用并保留数据副本再排查。

## 剩余风险

- SQLite、快照、图片和备份默认未加密；磁盘加密、系统账号和备份介质安全由用户负责。
- 网页正文、Markdown、压缩数据、图片解码、Chromium、Node/Tauri 和依赖可能存在未知漏洞。
- 公网站点可以提供极慢、动态变化或针对提取器定制的内容；提取准确性不是安全真实性保证。
- 本地或远程模型可能泄露、保留或错误处理已确认发送的数据，也可能产生虚假内容。
- 当前没有签名自动更新或撤回通道；版本获取和回退依赖人工核对 Git 提交、发布说明与数据备份。
- 物理访问、已入侵的同用户进程、系统钥匙串或操作系统失陷不在应用可防御范围内。

## 报告安全问题

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密渠道（若该入口可用）：

<https://github.com/SaraiNoQ/web-knowledge-set/security/advisories/new>

如果私密入口不可用，请只创建一个不含漏洞细节的普通 issue，请维护者提供安全联系方式。报告中说明受影响版本/提交、影响、最小复现步骤和建议修复；不要附加真实知识库、数据库、备份、快照、诊断包、Cookie、令牌、API 密钥或其他个人数据。获得私密通道后再传递已脱敏材料。

正式发布前不承诺固定响应时限，但会优先处理可导致代码执行、数据泄露、SSRF 绕过、认证绕过、更新供应链破坏或不可恢复数据丢失的问题。
