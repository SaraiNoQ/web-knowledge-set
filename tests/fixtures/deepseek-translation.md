# A small public knowledge note

- Keep useful pages in one searchable library.
- Review every generated result before using it.

Read the [public guide](https://example.com/guides?source=zhiye#safe) and run `pnpm verify` before release.

```sh
printf '%s\n' 'keep this code unchanged'
```
